# Fonts

Drop `.ttf` or `.otf` files here.

Free game fonts: [Google Fonts](https://fonts.google.com/) (most are OFL/Apache, suitable for distribution).

After importing, set as default font in Project Settings → GUI Theme.
